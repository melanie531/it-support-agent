# CLAUDE.md — IT Support Agent

> Auto-generated by AIDLC Inception. Do not edit manually.

## Project Overview

**Repository:** melanie531/it-support-agent  
**Complexity:** simple  

## Tech Stack

- **Language:** Python 3.11+
- **Testing:** pytest + pytest-asyncio

## Architecture Constraints

- No hardcoded secrets
- All user inputs validated

## Testing Standards

- **TDD Required:** Write failing test FIRST, then implement
- Minimum coverage: 80%
- Use pytest fixtures, not test setup/teardown methods
- Mock external services in unit tests
- Test file naming: `tests/test_{module_name}.py`

## Coding Standards

- PEP 8, max line length 120
- Type hints on all function signatures
- Docstrings on all public functions (Google style)
- Use `pathlib.Path` over `os.path`
- No bare `except:` — always catch specific exceptions
- Use `logging` module, not `print()`

## Enforcement Rules

The following `.claude/rules/` files enforce these standards:

- `.claude/rules/tdd-rule.md` — Write failing tests before implementation
- `.claude/rules/spec-compliance.md` — All code must trace to spec.md AC
